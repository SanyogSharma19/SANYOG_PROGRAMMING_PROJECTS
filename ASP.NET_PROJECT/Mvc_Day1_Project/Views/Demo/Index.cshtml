
@{
    //Layout = null;

    string[] namesArray = new string[] { "Name1", "Name2", "Name3", "WOW" };
    int count = 10;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        <p>@DateTime.Now.ToShortDateString()</p>
        @foreach (var item in namesArray)
        {

            <p>@item</p>
        }

        <ol>
            @for (int i = 0; i < namesArray.Length; i++)
            {

                <li>@namesArray[i]</li>

            }
        </ol>

        </div>
    <div>

        @if(count > 5)
            {

        <p>Senior Consultant</p>
            }

else
            {
                <p>  Consultant    </p>
            }
    </div>
</body>
</html>
